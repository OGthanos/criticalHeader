{
  "name": "criticalheader",
  "version": "1.0.0",
  "description": "request from the browser his critical headers and provide them back to the response",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  }
}